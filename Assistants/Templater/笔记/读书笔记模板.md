---
multiMedia: 
id: <% tp.date.now("YYYYMMDDHHmmss") %>
发行日期: 
multiFile: 
tags:
  - 模板
  - 读书笔记
fileName: " "
author: " "
出版社: " "
imageData:
cssclasses: 
---

``````ad-flex 
````col-md
flexGrow=1
===
```meta-bind-embed
[[书籍信息模块3]]
```
````

````col-md
flexGrow=1
===
✏️Thoughts/读后感 
```meta-bind-embed
[[横格输入模块-textArea-4行]]
```
````
``````

<%*
let file = app.workspace.getActiveFile();
if (!file) {
    throw new Error("没有活动的文件");
}
let newFolderPath = file.parent.path + "/" + file.basename;
let newFilePath = newFolderPath + "/" + file.name;
let confirmed = await tp.system.suggester(
    ["是", "否"], 
    [true, false], 
    false, 
    `是否将文件移动到同名文件夹:\n${newFilePath}?`
);

if (confirmed) {
    if (!app.vault.getAbstractFileByPath(newFolderPath)) {
        await app.vault.createFolder(newFolderPath);
    }
    await app.fileManager.renameFile(file, newFilePath);
    let success = await app.commands.executeCommandById("dataview:dataview-rebuild-current-view");
}
%>

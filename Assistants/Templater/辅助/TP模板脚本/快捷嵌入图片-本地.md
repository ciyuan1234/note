<%*
const settings = {
    size: [
        "150x150",
        "200x200",
        "280x280",
        "490x300",
        "500x500",
        "490x700",
        "720x400"
    ],
    useMask: true
}
const LocalImagePicker = tp.user.LocalImagePicker;
const imageInstance = new LocalImagePicker(tp, tR.trim(), settings);
tR = await imageInstance.makeImage();
-%>
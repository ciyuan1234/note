---
fileName: 一个同时创建文件夹、文件的按钮
multiFile: 
multiMedia:
  - "null"
description: "null"
tags:
  - 指定文件夹
  - 文件名
  - 按钮
  - 模块
createTime: 2024-07-14 21:37
id: 202407143746
area:
---

```js-engine
/**
 * Initial Setup 
 **/
// Summoning Meta Bind
const mb = engine.getPlugin('obsidian-meta-bind-plugin').api;

/**
 * Function to create buttons
 **/
function createButton(lb, t, i, fn, fdp) {
    const buttonConfig = {
        label: lb,
        icon: 'activity', // Lucide.dev 'activity' icon's name
        style: 'primary',
        tooltip: t,
        id: i,
        hidden: false, // Make the button visible
        actions: [
            { type: 'inlineJS', code: `!app.vault.getFolderByPath('${fdp}') ? await app.vault.createFolder('${fdp}') : '';` },
            { type: 'createNote', fileName: fn, folderPath: fdp, openNote: true }
        ]
    };
    const buttonOptions = {
        declaration: buttonConfig,
        position: undefined,
        isPreview: false
    };
    const button = mb.createButtonMountable(context.file.path, buttonOptions);
    return mb.wrapInMDRC(button, container, component);
};

/**
 * JS Engine Single Button Setup 
 **/
const rowNumber = '01'; // Last row number
const fileName = `项目 ${rowNumber}`;
const folderPath = `Documents/I.P.A.R.A/${fileName}`;
const buttonLabel = `Create ${fileName}`;
const buttonTooltip = `... in ${folderPath}`;
const buttonId = `MBCreateNote${rowNumber}`;

const button = createButton(buttonLabel, buttonTooltip, buttonId, fileName, folderPath);

/**
 * Convert button to HTML
 **/
const buttonHtml = button.outerHTML;

/**
 * Return the button HTML as the output
 **/
return buttonHtml;
```


%%
📃使用说明：
可以在其他markdown文件中输入/粘贴以下代码块以使用本模块；
````右上角复制
```meta-bind-embed
[[同时创建文件夹、文件名称的按钮]]
```
````
输入快捷操作：alt+1
复制粘贴快捷操作：右上角图标复制，然后粘贴
本模板被嵌套到其他文档中后；
- 点击`按钮`在指定路径下新建文件夹及笔记；
- 默认路径`Documents/I.P.A.R.A`、默认名称`项目+number`
- 点击模块右上角的</>符号修改本模板；
- 点击此处👉[视频课](https://img-1319209135.cos.ap-chongqing.myqcloud.com/202406211223554.png)咨询、学习进阶课程；
🚨提示：
- 注1：当修改了本模板后，被嵌入在其他文档的内容也会跟随调整；
- 注2：本使用说明中的注释内容不会被嵌套到其他文件中；
%%
---
fileName: 电子书架模板
tags:
  - 模块
  - 未读
  - 电子书架
multiFile: 
cssclasses:
  - cards
  - cards-cover,
  - cards-1-1,
  - table-max,
  - cards-cols-4
---

```dataview
Table without id embed(link(imageData)) ,"![](" + imageData + ")" as Cover,
file.link as title, "*作者：" + author + "*" , "**进度：" + progress + "%**","评价：" + 评价 + "",description
From "Books"
Where contains(status, "未读") 
```

%%
📃使用说明：
可以在其他markdown文件中输入/粘贴以下代码块以使用本模块；
````右上角复制
```meta-bind-embed
[[电子书架模块（未读）]]
```
````
输入快捷操作：alt+1
粘贴快捷操作：右上角图标复制，然后粘贴
本模块被嵌套到其他文档中后，若要实现卡片样式；
- 需要在fontmatter中添加属性`cssclasses: cards`
- 需要在`设置-外观-css代码片段`中启用`minimalcards`代码片段（minimal主题除外）
- 支持网络图片、本地图片（引用、路径）
- 点击模块右上角的</>符号修改本模块（显示内容、大小、字体、其他样式等）；
- 点击此处👉[视频课](https://img-1319209135.cos.ap-chongqing.myqcloud.com/202406211223554.png)咨询、学习进阶课程；
🚨提示：
- 注1：当修改了本模块样式后，被嵌入在其他文档的样式（不含输入内容）也会跟随调整；
- 注2：本使用说明中的注释内容不会被嵌套到其他文件中；
%%
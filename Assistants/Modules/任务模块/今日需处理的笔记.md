---
createTime: 2025-03-12 23:04
笔记ID: 20250312230452
笔记类型: 
阐述日期: 
tags: 
aliases: 
cssclasses: 
---

📆`INPUT[date:今日日期]` [[homepage-主页-卡片盒笔记|🔙数据库]]   
### 今日需要处理的笔记（Today）  
```dataviewjs
const folderPath = "Documents";
const today = dv.date("today").startOf("day");
function parseFrontmatterDate(content) {
    const match = content.match(/阐述日期:\s*(\d{4}-\d{2}-\d{2})/);
    return match ? dv.date(match[1])?.startOf("day") : null;
}
async function processFile(file) {
    try {
        const content = await app.vault.cachedRead(file);
        const expoundDate = parseFrontmatterDate(content);
        if (!expoundDate?.equals(today)) return null;
        const progressMatches = [...content.matchAll(
            /<progress\b[^>]*value\s*=\s*["'](\d+)["'][^>]*max\s*=\s*["'](\d+)["'][^>]*>/gi
        )];
        // 过滤掉value等于max的进度条（即完成的任务）
        const filteredBars = progressMatches.filter(m => {
            const value = parseInt(m[1]);
            const max = parseInt(m[2]);
            return value < max;
        }).map(m => m[0]);
        return filteredBars.length > 0 ? {
            name: file.basename,
            path: file.path,
            date: expoundDate,
            bars: filteredBars
        } : null;
    } catch (e) {
        console.error("处理文件错误:", file.path, e);
        return null;
    }
}
const files = app.vault.getMarkdownFiles()
    .filter(f => f.path.startsWith(folderPath + "/"));
const results = (await Promise.all(files.map(processFile)))
    .filter(Boolean)
    .sort((a, b) => a.date - b.date);
    dv.container.className += " custom-dataview-table";
const style = document.createElement('style');
style.textContent = `
.custom-dataview-table .table-view-table th:nth-child(2),
.custom-dataview-table .table-view-table td:nth-child(2),
.custom-dataview-table .table-view-table th:nth-child(3),
.custom-dataview-table .table-view-table td:nth-child(3) {
    width: 18%;
}
.custom-dataview-table .table-view-table th:nth-child(1),
.custom-dataview-table .table-view-table td:nth-child(1) {
    width: 64%;
}
`;
document.head.appendChild(style);
dv.table(
    ["笔记名称", "计划日期", "笔记进度"],
    results.flatMap(item => 
        item.bars.map(bar => [
            dv.fileLink(item.path, false, item.name), 
            item.date.toISODate(),
            bar
        ])
    )
);
```

### 未来一周待处理笔记（OneWeek）  
```dataviewjs
const folderPath = "Documents";
const today = dv.date("today").startOf("day");
const startDate = today.plus({ days: 1 });
const endDate = today.plus({ days: 7 });
function parseFrontmatterDate(content) {
    const match = content.match(/阐述日期:\s*(\d{4}-\d{2}-\d{2})/);
    return match ? dv.date(match[1])?.startOf("day") : null;
}
async function processFile(file) {
    try {
        const content = await app.vault.cachedRead(file);
        const expoundDate = parseFrontmatterDate(content);
        if (!expoundDate || expoundDate < startDate || expoundDate > endDate) return null;
        const progressMatches = [...content.matchAll(
            /<progress\b[^>]*value\s*=\s*["'](\d+)["'][^>]*max\s*=\s*["'](\d+)["'][^>]*>/gi
        )];
        // 过滤掉value等于max的进度条（即完成的任务）
        const filteredBars = progressMatches.filter(m => {
            const value = parseInt(m[1]);
            const max = parseInt(m[2]);
            return value < max;
        }).map(m => m[0]);
        return filteredBars.length > 0 ? {
            name: file.basename,
            path: file.path,
            date: expoundDate,
            bars: filteredBars
        } : null;
    } catch (e) {
        console.error("处理文件错误:", file.path, e);
        return null;
    }
}
const files = app.vault.getMarkdownFiles()
    .filter(f => f.path.startsWith(folderPath + "/"));
const results = (await Promise.all(files.map(processFile)))
    .filter(Boolean)
    .sort((a, b) => a.date - b.date);
    dv.container.className += " custom-dataview-table";
const style = document.createElement('style');
style.textContent = `
.custom-dataview-table .table-view-table th:nth-child(2),
.custom-dataview-table .table-view-table td:nth-child(2),
.custom-dataview-table .table-view-table th:nth-child(3),
.custom-dataview-table .table-view-table td:nth-child(3) {
    width: 18%;
}
.custom-dataview-table .table-view-table th:nth-child(1),
.custom-dataview-table .table-view-table td:nth-child(1) {
    width: 64%;
}
`;
document.head.appendChild(style);
dv.table(
    ["笔记名称", "计划日期", "笔记进度"],
    results.flatMap(item => 
        item.bars.map(bar => [
            dv.fileLink(item.path, false, item.name), 
            item.date.toISODate(),
            bar
        ])
    )
);
```

### 已逾期未处理的笔记（Overdue）  
```dataviewjs
const folderPath = "Documents";
const today = dv.date("today").startOf("day");
function parseFrontmatterDate(content) {
    const match = content.match(/阐述日期:\s*(\d{4}-\d{2}-\d{2})/);
    return match ? dv.date(match[1])?.startOf("day") : null;
}
async function processFile(file) {
    try {
        const content = await app.vault.cachedRead(file);
        const expoundDate = parseFrontmatterDate(content);
        if (!expoundDate || expoundDate >= today) return null;
        const progressMatches = [...content.matchAll(
            /<progress\b[^>]*value\s*=\s*["'](\d+)["'][^>]*max\s*=\s*["'](\d+)["'][^>]*>/gi
        )];
        // 过滤掉value等于max的进度条（即完成的任务）
        const filteredBars = progressMatches.filter(m => {
            const value = parseInt(m[1]);
            const max = parseInt(m[2]);
            return value < max;
        }).map(m => m[0]);
        return filteredBars.length > 0 ? {
            name: file.basename,
            path: file.path,
            date: expoundDate,
            bars: filteredBars
        } : null;
    } catch (e) {
        console.error("处理文件错误:", file.path, e);
        return null;
    }
}
const files = app.vault.getMarkdownFiles()
    .filter(f => f.path.startsWith(folderPath + "/"));
    
const results = (await Promise.all(files.map(processFile)))
    .filter(Boolean)
    .sort((a, b) => a.date - b.date); 
dv.container.className += " custom-dataview-table";
const style = document.createElement('style');
style.textContent = `
.custom-dataview-table .table-view-table th:nth-child(2),
.custom-dataview-table .table-view-table td:nth-child(2),
.custom-dataview-table .table-view-table th:nth-child(3),
.custom-dataview-table .table-view-table td:nth-child(3) {
    width: 18%;
}
.custom-dataview-table .table-view-table th:nth-child(1),
.custom-dataview-table .table-view-table td:nth-child(1) {
    width: 64%;
}
`;
document.head.appendChild(style);
dv.table(
    ["笔记名称", "计划日期", "笔记进度"],
    results.flatMap(item => 
        item.bars.map(bar => [
            dv.fileLink(item.path, false, item.name),
            item.date.toISODate(),
            bar
        ])
    )
);
```

%%说明
本模块查询的笔记中必须包含**阐述日期**、**进度条**，缺少任意一个条件，笔记将不在此页面中显示
可以通过alt(opt)+x查询，并点击提示框进入到本页面
%%
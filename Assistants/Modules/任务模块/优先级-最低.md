
```tasks
not done
priority is lowest
sort by due
short mode
hide task count
```
%%
- 如果是空白，说明没有创建优先级任务
- 此模板与 [[优先级-最低]]模块关联，若需要修改此模板，请跳转到上述模块中。
- 此模板使用需要配合任务优先级符号使用：通过ctrl+p领料面板，找到Tasks: Create or edit task命令创建任务，并添加优先级符号，当任务打上了⏬符号后，任务将自动归集到此模板中
%%
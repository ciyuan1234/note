
```js-engine
const mb = engine.getPlugin('obsidian-meta-bind-plugin').api;

const tableOptions = {
	bindTarget: mb.createBindTarget('frontmatter', context.file.path, ['contact今日事件']),
	tableHead: ['起始', '结束', '事件'],
	columns: [
		'INPUT[time:scope^起始时间]',
		'INPUT[time:scope^结束时间]',
		'INPUT[textArea(class(textArea-90)):scope^今日事件]',
	],
};

const mountable = mb.createTableMountable(context.file.path, tableOptions);

mb.wrapInMDRC(mountable, container, component);

```
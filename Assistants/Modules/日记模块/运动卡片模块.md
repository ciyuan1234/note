---
fileName: 运动卡片模块
tags:
  - 模块
  - 运动
multiFile: 
运动时长: 
体重:
---

``````ad-habit
title: 每日运动
````col
```col-md
⛹️ *健康生活，从运动开始；美好人生，从健康开始~~*
❓ 做到了吗 `INPUT[inlineSelect(option(1,是), option(0,否)):运动打卡]`
💪 运动项目 `INPUT[inlineSelect(option(2, 慢走), option(3, 步行), option(3.3, 扫地拖地), option(3.4, 瑜伽), option(5, 网球), option(5.5, 举重), option(5.9, 徒步), option(6, 舞蹈), option(6.4, 骑行（平地）), option(14, 骑行（山地）), option(6.5, 分腿跳跃), option(7.2, 拳击（打沙包）), option(9.5, 拳击), option(7.7, 开合跳), option(8, 篮球), option(10, 游泳), option(8.8, 慢跑（较慢）), option(11, 慢跑（较快）), option(10.1, 跳绳), option(10.2, 足球)):METs值]` 
⏰ 运动时长 `INPUT[number(class(meta-bind-small-width)):运动时长]` 小时
🎛️ 今日体重 `INPUT[number(class(meta-bind-small-width)):体重]` kg（运动后体重）    
💦 消耗热量 `VIEW[round({体重} * {运动时长} * {METs值}* {运动打卡})]` 大卡(MET值法，取整)




 ```
````
``````

%%
📃使用说明：
可以在其他markdown文件中输入/粘贴以下代码块嵌入以使用本模板；
````右上角复制
```meta-bind-embed
[[运动卡片模块]]
```
````
输入快捷操作：alt+1
粘贴快捷操作：右上角图标复制，然后粘贴
本模块被嵌套到其他文档中后；
- 点击`输入框`输入内容。添加的内容会绑定到前页frontmatter（文档属性/元数据/metadata）中，如果未显示，可以在`设置-编辑器-文档属性`开启它；
- 点击模块右上角的</>符号修改本模块（颜色、字体、长宽、布局等）；
- 点击此处👉[视频课](https://img-1319209135.cos.ap-chongqing.myqcloud.com/202406211223554.png)咨询、学习进阶课程；
🚨提示：
- 注1：当修改了本模块样式后，被嵌入在其他文档的样式（不含输入内容）也会跟随调整；
- 注2：本使用说明中的注释内容不会被嵌套到其他文件中；
%%
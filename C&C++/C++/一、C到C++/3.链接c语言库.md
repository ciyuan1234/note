# 一、思考
C++编译器在编译代码时候，会使用**函数名+参数类型**的方式找对应的函数调用，**而C语言编译器编译生成的库种函数名是没有类型组合在一起的**，==此时在C++代码中直接调用C语言库中函数可以调用成功吗？==
# 二、动手试试
```cpp
//gcc编译生成动态库
gcc -foic -shared add.c sub.c -o libas.so
//链接第三方库
g++ main.cpp -L . -las
```
C++调用C语言的库（头文件写法）：
![[Pasted image 20260429184729.png]]
加上extern“C”   